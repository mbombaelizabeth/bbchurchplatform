<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                              https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Church Notification Platform</display-name>

    <welcome-file-list>
        <welcome-file>login.jsp</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!--
        RegistrationServlet, LoginServlet, LogoutServlet and
        SendNotificationServlet (@WebServlet), the WebSocket endpoint
        NotificationEndpoint (@ServerEndpoint), the RBAC filter
        AccessControlFilter (@WebFilter), and the JMS resources / MDB
        JMSConfig + NotificationMessageListener (@JMSConnectionFactoryDefinition,
        @JMSDestinationDefinition, @MessageDriven) are all configured via
        annotations in their respective classes, so no further
        declarations are required in this file.
    -->

</web-app>
